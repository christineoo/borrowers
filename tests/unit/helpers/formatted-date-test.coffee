`import { formattedDate } from '../../../helpers/formatted-date'`
`import { module, test } from 'qunit'`

module 'FormattedDateHelper'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = formattedDate 42
  assert.ok result
